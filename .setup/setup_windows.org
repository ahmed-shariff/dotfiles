- Configure environment variable $env:HOME=%USERPROFILE%
- Run the setup script from github
- install msys2
- in msys2 pacman -Sy base-devel gcc
- add msys64/usr/bin to path
~winget install emacs~
~winget install git --source winget~
~winget install slack --source winget~
~winget install -e --id Unity.UnityHub~
~choco install ripgrep~
- configure ssh in git-bash https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account
- clone org
~winget install JanDeDobbeleer.OhMyPosh -s winget~
- install font for oh my posh (FiraCode)
- download and install iosveka-aile fonts
~choco install firacode~
~winget install 7zip --source winget~
~winget install -e --id SyncTrayzor.SyncTrayzor~
- install python
- copy and rename python.exe to python311.exe
~winget install -e --id JohnMacFarlane.Pandoc~
